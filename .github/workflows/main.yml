on:
 push:
  branches:
    -master 



    jobs:
     build:
      runs-on:windo

        steps:
        - name: 'testing github action'
           run: pwd
         - name : 'testing printing'
          run: echo Ramadan

